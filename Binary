
            int x = -1;
            for (int i = 0; i < 2; i++)
            {
                char[] s = Console.ReadLine().ToCharArray();
                int b = 0;
                foreach (var c in  s)
                {
                    b |= 1 << c - 'a';
                }
                x &= b;
            }
